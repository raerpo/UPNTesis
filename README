Plantilla y clase para la realización de trabajos de grado de la universidad pedagógica nacional.

Recomendaciones:

-> Si la plantilla con compila es probable que falten los paquetes latex necesarios. La plantilla fue creada en Ubuntu 12.04 con la siguiente instalación de latex:

sudo aptitude install texlive-latex-base texlive-latex-recommended texlive-latex-extra

-> Cuando se compila por primera vez no aparecen ni las referencias cruzadas ni la tabla de contenido. Para solucionarlo simplemente vuelva a compilar.

Versiones:

-> 0.1 : Primera versión del la clase y de la tesis.
